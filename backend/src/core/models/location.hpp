#pragma once
#include <string>

struct LocationRecord
{
    std::string id;
    std::string name;
    std::string created_at;
    std::string updated_at;
};

struct MaterialNameRecord
{
    std::string id;
    std::string name;
    std::string created_at;
    std::string updated_at;
};
